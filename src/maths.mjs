export function add(a, b) {
    return b + a;
}

function subtract(a, b){
    return b-a;
}

//default export can only be one per file
// regular exports can be as much as possible
export default subtract;

